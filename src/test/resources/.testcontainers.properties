# TestContainers Docker Configuration
# Auto-detection enabled for all Docker client strategies

testcontainers.reuse.enable=false
testcontainers.debug=true

# Let TestContainers auto-detect Docker client strategy
# Default strategies in order: LoadFromEnvironment -> WithDockerSocket -> WithDockerCmdExec
# No explicit strategy specified to allow auto-detection
